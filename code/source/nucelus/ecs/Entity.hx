package nucelus.ecs;

// Super-charged entity with easy access to stuff,
// eg. .sprite(...) adds a sprite component/system as necessary.
class Entity extends BaseEntity
{
    public function new()
    {
        super();
    }
}